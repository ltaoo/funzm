export default class User {
  /**
   * 注册
   * @param credentials
   */
  static register(credentials) {}
  /**
   * 登录   * @param credentials
   */
  static login(credentials) {}

  forgotPassword() {}

  resetPassword() {}
}
