generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = "mysql://litao:Li1218040201@rm-bp1rzk2qoex2351o6vo.mysql.rds.aliyuncs.com:3306/caption-learning"
}

model Account {
  id                 String  @id @default(cuid())
  userId             String  @map("user_id")
  type               String
  provider           String
  providerAccountId  String  @map("provider_account_id")
  refresh_token      String?
  access_token       String?
  expires_at         Int?
  token_type         String?
  scope              String?
  id_token           String?
  session_state      String?

  user               User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
  @@map("accounts")
}

model User {
  id            String    @id @default(cuid())
  name          String?
  email         String?   @unique
  emailVerified DateTime? @map("email_verified")
  avatar        String?
  password      String
  salt          String

  accounts      Account[]
  sessions      Session[]
  captions      Caption[]
  exams         Exam[]
  notebooks     Notebook[]

  created_at    Int
  last_updated  Int?

  @@map("users")
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique @map("session_token")
  userId       String   @map("user_id")
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("sessions")
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
  @@map("verificationtokens")
}

// model Email {
//   id            Int     @id @default(autoincrement())
//   userUid       String
//   email         String  @unique
// }

model Caption {
  id           String        @id @default(cuid())
  title        String
  content      String?       @db.LongText
  published    Boolean       @default(false)
  publisher    User?         @relation(fields: [publisherId], references: [id])
  publisherId  String?       @map("publisher_id")
  paragraphs   Paragraph[]

  exams        Exam[]

  movieId      String?       @map("movie_id")

  created_at    Int
  last_updated  Int?
}

model Paragraph {
  id           String   @id @default(cuid())
  line         Int
  text1        String
  text2        String
  start        String
  end          String
  language     String?

  captionId    String   @map("caption_id")
  caption      Caption  @relation(fields: [captionId], references: [id], onDelete: Cascade)

  // 该段落被拼写错误过的记录
  errors       Error[]
  // 该段落有哪些单词被记录进单词本
  words        Word[]

  @@map("paragraphs")
}

model Exam {
  id           String    @id @default(cuid())

  status       Int       @default(1)

  captionId    String    
  caption      Caption   @relation(fields: [captionId], references: [id], onDelete: Cascade)

  user         User?     @relation(fields: [userId], references: [id])
  userId       String?   @map("publisher_id")

  curParagraphId String? @map("cur_paragraph_id")
  combo          Int     @default(0)
  maxCombo       Int     @default(0) @map("max_combo")

  skippedParagraphIds   String?       @map("skipped_paragraph_ids") @db.LongText
  correctParagraphIds   String?       @map("correct_paragraph_ids") @db.LongText
  incorrectParagraphIds String?       @map("incorrect_paragraph_ids") @db.LongText

  errors         Error[]

  created_at    Int
  last_updated  Int?

  @@map("exams")
}

model Error {
  id           String     @id @default(cuid())

  paragraphId  String     @map("paragraph_id")
  paragraph    Paragraph  @relation(fields: [paragraphId], references: [id], onDelete: Cascade)

  examId  String     @map("exam_id")
  exam    Exam       @relation(fields: [examId], references: [id], onDelete: Cascade)

  inputting    String    

  created_at    Int

  @@map("errors")
}

model Notebook {
  id                String     @id @default(cuid())

  title             String?

  user              User?     @relation(fields: [userId], references: [id])
  userId            String?   @map("publisher_id")

  words             Word[]

  created_at    Int
  last_updated  Int?

  @@map("notebooks")
}

model Word {
  id                    String     @id @default(cuid())

  text                  String

  notebookId            String
  notebook              Notebook?     @relation(fields: [notebookId], references: [id])

  paragraphId           String                  
  paragraph             Paragraph     @relation(fields: [paragraphId], references: [id])

  created_at            Int

  @@map("words")
}

